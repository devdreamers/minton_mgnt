import { requireApprovedAdmin } from "@/lib/auth/require-admin";
import { listApplicationsForSession, listSessionTemplates, listUpcomingSessions } from "@/lib/services/session-service";
import { cancelSessionAction, createInstanceAction, createTemplateAction } from "./actions";

const days = ["일요일", "월요일", "화요일", "수요일", "목요일", "금요일", "토요일"];
export const dynamic = "force-dynamic";

export default async function AdminSessionsPage() {
  await requireApprovedAdmin();
  const [templates, sessions] = await Promise.all([listSessionTemplates().catch(() => []), listUpcomingSessions().catch(() => [])]);
  const applicationEntries = await Promise.all(sessions.slice(0, 10).map(async (session) => [session.id, await listApplicationsForSession(session.id).catch(() => [])] as const));
  const applications = new Map(applicationEntries);
  return <div className="stack"><section className="card"><span className="eyebrow">Session templates</span><h1 style={{ marginTop: 12 }}>소모임 템플릿</h1><form className="form" action={createTemplateAction} style={{ marginTop: 20 }}><div className="grid cols-3"><div className="field"><label>소모임명</label><input name="title" placeholder="수요일 정모" required /></div><div className="field"><label>요일</label><select name="day_of_week" defaultValue="3">{days.map((day, index) => <option value={index} key={day}>{day}</option>)}</select></div><div className="field"><label>정원</label><input name="capacity" type="number" min={1} defaultValue={16} required /></div></div><div className="grid cols-3"><div className="field"><label>시작</label><input name="start_time" type="time" defaultValue="19:00" required /></div><div className="field"><label>종료</label><input name="end_time" type="time" defaultValue="22:00" required /></div><div className="field"><label>신청 오픈</label><input name="application_open_time" type="time" defaultValue="10:00" required /></div></div><button className="btn btn-primary" type="submit">템플릿 저장</button></form><div style={{ overflowX: "auto", marginTop: 24 }}><table className="table"><thead><tr><th>이름</th><th>요일/시간</th><th>정원</th><th>신청 오픈</th></tr></thead><tbody>{templates.length === 0 ? <tr><td colSpan={4} className="subtle">등록된 템플릿이 없습니다.</td></tr> : templates.map((template) => <tr key={template.id}><td>{template.title}</td><td>{days[template.day_of_week]} {template.start_time.slice(0, 5)}–{template.end_time.slice(0, 5)}</td><td>{template.capacity}명</td><td>{template.application_open_time.slice(0, 5)}</td></tr>)}</tbody></table></div></section><section className="card"><span className="eyebrow">Session instances</span><h2 style={{ marginTop: 12 }}>회차 생성</h2><p className="muted">시각 입력은 한국 표준시로 저장됩니다.</p><form className="form" action={createInstanceAction} style={{ marginTop: 20 }}><div className="grid cols-3"><div className="field"><label>템플릿</label><select name="template_id" required>{templates.map((template) => <option key={template.id} value={template.id}>{template.title}</option>)}</select></div><div className="field"><label>날짜</label><input name="session_date" type="date" required /></div><div className="field"><label>정원</label><input name="capacity" type="number" min={1} defaultValue={templates[0]?.capacity ?? 16} required /></div></div><div className="grid cols-3"><div className="field"><label>시작 시각</label><input name="start_at" type="datetime-local" required /></div><div className="field"><label>종료 시각</label><input name="end_at" type="datetime-local" required /></div><div className="field"><label>신청 오픈 시각</label><input name="application_open_at" type="datetime-local" required /></div></div><button className="btn btn-primary" type="submit">회차 생성</button></form><div style={{ overflowX: "auto", marginTop: 24 }}><table className="table"><thead><tr><th>회차</th><th>신청자</th><th>상태</th><th>작업</th></tr></thead><tbody>{sessions.length === 0 ? <tr><td colSpan={4} className="subtle">생성된 회차가 없습니다.</td></tr> : sessions.map((session) => { const apps = applications.get(session.id) ?? []; return <tr key={session.id}><td><strong>{session.session_templates?.title ?? "소모임"}</strong><div className="subtle">{session.session_date} · {new Date(session.start_at).toLocaleTimeString("ko-KR", { hour: "2-digit", minute: "2-digit" })}</div></td><td>{apps.filter((application) => application.status === "confirmed").length}/{session.capacity}명<div className="subtle">대기 {apps.filter((application) => application.status === "waitlisted").length}명</div></td><td>{session.status}</td><td>{session.status === "scheduled" ? <form action={cancelSessionAction}><input type="hidden" name="session_id" value={session.id} /><button className="btn" type="submit">회차 취소</button></form> : "-"}</td></tr>; })}</tbody></table></div></section></div>;
}
